using UnityEngine;
using System.Collections;

public class RotateMe : MonoBehaviour
{
    Vector3 myRot = new Vector3(1, 0, 1);

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update ()
    {
        //gameObject.transform.Rotate(myRot);
	
	}
}
