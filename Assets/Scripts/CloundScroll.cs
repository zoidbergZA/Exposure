using UnityEngine;
using System.Collections;

public class CloundScroll : MonoBehaviour {

    public float scrollSpeed;
    public string texName;

    Material objMat;
    float offsetX;
    float offsetY;
    vector2 offset;
    Renderer renderer;
	// Use this for initialization
	void Start () {
	    renderer = GetComponent 
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
